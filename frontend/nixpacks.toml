[phases.install]
cmds = [
    # Install npm dependencies
    "npm ci"
]

[phases.build]
dependsOn = ["install"]
cmds = [
    # Build Next.js (proto files are pre-generated and committed)
    "npm run build"
]

[start]
cmd = "npm run start"
