# Railway configuration for monorepo deployment
# Deploy each service separately by pointing to this repo

[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

